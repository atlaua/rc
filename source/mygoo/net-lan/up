s6-envdir /etc/s6/env/host/net/lan
multisubstitute
{
    importas -isu addr addr
    importas -iu dev dev
    importas -iu gateway gateway
}

if { ip addr replace dev ${dev} ${addr} }
if { ip link set dev ${dev} up }
if { ip route replace default via ${gateway} }
